[build]
  command = "npm run build"
  publish = "dist"        # set to your actual output folder (dist, public, build, etc)
  functions = "functions"  # optional

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true